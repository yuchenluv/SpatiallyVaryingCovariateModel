# Eventually we can make this `const` to make things run faster but for now no need
# These are all the parameters we want to store
PARAMS = (
    latmin = 27.0,
    lonmin = -97.0,
    latmax = 31.0,
    lonmax = -86.5,
    min_obs_per_year = 362,
    min_years = 30,
)
